/**
 * Created by mryzhkouskaya on 22.05.2019.
 */

({
    handleCmpObjectChoiceEvent: function (component, event, helper) {

        helper.handleCmpObjectChoiceEvent(component, event, helper);
    },

    handleCmpSelectedFieldsEvent: function (component, event, helper) {

        helper.handleCmpSelectedFieldsEvent(component, event, helper);
    }
});